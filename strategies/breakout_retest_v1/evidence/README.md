# Breakout Retest V1 Evidence

Runtime approval evidence is local-only and remains under `state/backtest-approved.json`.
This directory is reserved for non-secret strategy lifecycle notes and reviewable evidence
indexes. Do not store Deriv tokens, `.env`, account artifacts, or raw runtime state here.

