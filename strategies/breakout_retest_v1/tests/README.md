# Breakout Retest V1 Tests

Current coverage lives in the repo-level harness:

- `tests/integration.js`
- `tests/backtestValidator.js`
- `tests/run-tests.js`

The manifest points to these files until strategy-specific test shards are split out.
